import { Moon } from "lucide-react";

function Theme() {
  return (
    <button>
      <span className="sr-only">Theme changer</span>
      <Moon size={24} />
    </button>
  );
}

export default Theme;
